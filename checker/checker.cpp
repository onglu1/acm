
#include <bits/stdc++.h>

#define Mid ((l + r) / 2)
#define lson (rt << 1)
#define rson (rt << 1 | 1)
using namespace std;
const int N = 2e6 + 1009;
int n, m, a[N], g[109][109];
signed main() {
    while(1) {
        system("rnd.exe>data.in");
        system("std.exe<data.in>data.out");
        system("bf.exe<data.in>data.ans");
        if(!system("fc data.out data.ans")) {
            break;
        }
    }
    return 0;
}